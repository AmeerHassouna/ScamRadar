# Node
node_modules/

# Claude Code session data
.claude/

# Python
__pycache__/
*.py[cod]
*.pyo
.Python
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
venv/
.venv/
env/

# Raw training data and outputs (large, not needed to run the API)
data/
outputs/

# Model development artefacts (frozen baselines + intermediate candidates).
# All historical v1.x model artifacts + backups now live under old_models/
# after the E5 model rollout. Kept locally for reproducibility;
# not shipped to production.
old_models/

# Training artefacts not loaded at inference (E7-P3 direction was rejected;
# the FAISS/embedding artifacts are not shipped with the deployed pipeline).
models/embeddings.npy
models/e7_p3_faiss/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp

# Streamlit
.streamlit/secrets.toml

# Local secrets
.env

# Defense-only material — intentionally kept outside the public software repository.
# The public repo ships only the code, the deployed model, deployment configs, and
# software documentation (README, SOURCE_OF_TRUTH, models/README, docs/USER_GUIDE).
# The four CRISP-DM notebooks, the thesis chapters, and the historical experiments/
# and models/_archive/ trees are maintained externally.
docs/thesis_*.md
notebooks/
scripts/notebooks/
experiments/
models/_archive/
