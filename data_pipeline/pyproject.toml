[project]
name = "scamradar-data-pipeline"
version = "0.1.0"
description = "ScamRadar+ data collection pipeline — acquire, clean, split, and audit external corpora"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
