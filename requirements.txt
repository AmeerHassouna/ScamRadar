# API server
fastapi==0.115.12
uvicorn[standard]==0.34.3
python-multipart==0.0.20
slowapi==0.1.9
python-dotenv==1.2.2
cachetools>=5.3

# ML inference
numpy
scipy
scikit-learn
# sentence-transformers (PyTorch) exceeds Render free-tier 512MB RAM limit;
# the pipeline degrades gracefully with proximity_score=0 when absent.
# faiss-cpu is also omitted for the same reason — it depends on ST for queries.


# Feature engineering
pandas
vaderSentiment==3.3.2
requests
python-whois
regex

# Auto-translation
langdetect==1.0.9
deep-translator==1.11.4
